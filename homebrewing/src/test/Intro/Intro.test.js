import { render, screen } from '@testing-library/react';
import {renderWithProviders} from '../util/reduxMock'
import Intro from '../../components/Intro/Intro'

describe('Get oldbrew list from database', () => {
    it('', () => {
        
    })
})